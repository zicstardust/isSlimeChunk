name: isSlimeChunk
version: $pluginVersion
main: com.zicstardust.isSlimeChunk.Main
api-version: $minecraftVersion
website: https://github.com/zicstardust/isSlimeChunk
author: zic_stardust
description: "Check if chunk spawn Slime"

default-permission: true

permissions:
  isSlimeChunk.slimechunk:
    description: "Permission command"

commands:
  slimechunk:
    description: "Check if chunk spawn Slime"
    usage: "/slimechunk"
    #aliases: [isslimechunk, slimechunk]
    permission: isSlimeChunk.slimechunk
    #permission-message: "You do not have permission to use this command"